# Environment
.env

# Personal, per-developer instructions — not shared with the team
CLAUDE.local.md

# Python
.venv/
__pycache__/
*.pyc

# Local dev database (sqlite default for backend/app/db)
backend/marketpulse.db
backend/marketpulse.db-journal

# Node
node_modules/

# Frontend build output
frontend/dist/

# GCP service account keys — never commit real credentials
marketpulse-saas-503410-*.json
*-service-account*.json
